# invalidSubscriberOrg

The subscriber org ID %s is invalid. It must be a 15- or 18-character ID that starts with "00D".

# invalidSubscriberPackage

The subscriber package ID %s is invalid. It must be a 15- or 18-character ID that starts with "033".

# noSubscriberOrgs

Provide at least one subscriber org ID.

# apiVersionTooLow

Package authorization requires API version %s or later.
