# apiVersionTooLow

Package link requires API version %s or later.

# missingOrgId

Unable to determine the target org ID from the current connection.

# invalidStatus

The status %s is invalid. Valid values are: pending, approved, declined, revoked.

# invalidVerifiedOrgId

The Verified Org ID %s isn't valid. Specify a valid organization ID (starts with 00D and is 15 or 18 characters) for the Verified Partner Business Org and retry the command.

# linkRequestAlreadyExists

A link request to Verified Org %s already exists with status '%s'. Run "sf package link unlink" to clear the current request before requesting a new link.

# linkRequestFailed

The link request to Verified Org %s couldn't be created. Verify that you're connected to an authoring org and that the Verified Org ID is correct, then retry the command.
